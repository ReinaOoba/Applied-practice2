<%= form_with(model: [book, book_comment], local: false) do |f| %>
  <!--<div id="comment_error">-->
  <!--  <#%= render "layouts/errors", obj: book_comment %>-->
  <!--</div>-->
  <%= f.text_area :comment, rows:'5',placeholder: "コメントをここに入力",class: "w-100"%>
  <%= f.submit "送信" %>
<% end %>